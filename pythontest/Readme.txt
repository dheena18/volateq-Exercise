On Test1.Py program i have implemented a restfulAPI program with python.With some required library i have generated a output
which display the Time,Date and Timezone of different countries with different languages

Procedure to run the program:
1)Clone the git hub repository
2)install the required libraries from the requirements.txt
3)Then use command python test1.py

###Important latest babel have some issues so download 1 version least or follow the requirements.txt liberarys###
###requirements.txt has some liberary which are not used in the project so it is not required to install everything###